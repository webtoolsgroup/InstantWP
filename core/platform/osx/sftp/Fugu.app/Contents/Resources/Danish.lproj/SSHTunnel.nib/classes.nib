{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                acceptHost = id; 
                addService = id; 
                authenticate = id; 
                cancelServiceAdd = id; 
                cancelTunnelCreation = id; 
                closeTunnel = id; 
                getNewTunnelSheet = id; 
                refuseHost = id; 
                selectFromFavorites = id; 
                selectService = id; 
                startTunnel = id; 
            }; 
            CLASS = SSHTunnel; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addServiceSheet = NSPanel; 
                addToKeychainSwitch = NSButton; 
                authProgBar = NSProgressIndicator; 
                authProgMsg = NSTextField; 
                connectMsg = NSTextField; 
                connectProgBar = NSProgressIndicator; 
                localPortField = NSTextField; 
                newServiceName = NSTextField; 
                newServicePort = NSTextField; 
                passErrorField = NSTextField; 
                passPromptField = NSTextField; 
                passpromptView = NSView; 
                passwordField = NSTextField; 
                popUpFavorites = NSPopUpButton; 
                remoteHostField = NSComboBox; 
                remotePortField = NSTextField; 
                serviceFavoritePopUp = NSPopUpButton; 
                sshTunnelInfoButton = NSPopUpButton; 
                sshtunnelSheet = NSPanel; 
                sshtunnelWindow = NSWindow; 
                tunnelCreationView = NSView; 
                tunnelHostField = NSTextField; 
                tunnelPortField = NSTextField; 
                unknownHostKeyField = NSTextField; 
                unknownHostMsgField = NSTextField; 
                unknownHostView = NSView; 
                usernameField = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}